<UserControl x:Class="MortuaryApp.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="ToastBorder" CornerRadius="8" Padding="14,10" Margin="0,0,0,8"
            BorderThickness="0" MaxWidth="380"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform x:Name="SlideTransform"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="IconText" Grid.Column="0" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="MessageText" Grid.Column="1" TextWrapping="Wrap" FontSize="12"
                       VerticalAlignment="Center" LineHeight="16"/>
            <TextBlock Grid.Column="2" Text="✕" FontSize="12" Margin="8,0,0,0"
                       VerticalAlignment="Center" Cursor="Hand" Opacity="0.6"
                       MouseLeftButtonDown="Close_Click"/>
        </Grid>
    </Border>
</UserControl>